# Fire and Lightning resistance input families

This owned-data successor adds two distinct modifier families using the existing native
schema, numerical rules and source prerequisites. It adds no evaluator opcode, interpreter,
PoB runtime dependency or build-specific dispatch. It is a component package; complete
native original-build coverage remains 0/5.

Apply it after the item-observations successor:

```text
poe-optimizer extend-owned-recipe PRIOR --extension data/owned/poe2/3887ae68/elemental-resistance-inputs/extension.json --items data/owned/poe2/3887ae68/elemental-resistance-inputs/items.json --item-source data/owned/poe2/3887ae68/elemental-resistance-inputs/item-source.json --output NEW
```

## Semantic data

The extension allocates 48 identities: Fire owner `292a`, Lightning owner `2942`, and 23
required input slots per owner. Keys have the `def.000000000000` prefix in the namespace
recorded in `bindings.json`. They retain distinct identities for later actor contributions;
shared arithmetic does not merge elemental channels or repeated occurrences on an item.

Each family carries a fresh raw amount, 20 explicit source-property Booleans, an ordinary
line unscalable flag and a corrupted-base factor. The raw fixed grammars preserve decimal
values without range-only formatting or prior nominal rounding. Parameter membership stays
Partial with the existing eligibility gap. The corrupted-base factor is explicitly one
only for the reviewed ordinary encoding; it is independent of an item's Corrupted marker.

Eight programs reuse the canonical Cold family's checked scalar, ordered-transform,
corrupted-base and numeric-component rules after rebinding only owner/slot identities.
Shared stat and option identities remain exact. Missing catalyst, ordered-transform or
eligibility producers remain unavailable; there is no fabricated identity magnitude or
actor-level contribution. All existing Partial program gaps remain attached to the new
owners. Numeric component output does not prove final source-cache or build parity.

All 1,756 constructed base templates gain explicit structural membership in both raw
families while retaining every old member and Partial closure. This does not assert legal
affix generation, active flask/charm effects or equipment applicability. The finite base
join and identity mappings are authoring evidence in `bindings.json`; the ordinary bounded
publisher validates the authored extension independently. Hashes are integrity evidence,
not a substitute for source validation.

## Source admission

Four injected grammars cover explicitly positive and negative Fire/Lightning text. Only
positive integer spellings have conditional independent-member proofs: a literal plus,
ASCII digits with raw value 0..1,000,000, no physical source tags and a uniquely recognized
base with no generated buff prefix. Negative and fractional fixed spellings can be decoded
as raw facts but remain unresolved in source attribution. Previous-line consumption,
unknown controls, duplicate or ambiguous bases and all existing source gates still apply.

The current shared property vocabulary includes generic `fire` and `lightning`, but not
the separate `fire_resistance` or `lightning_resistance` labels. No property is inferred
from the element's English name. The admitted empty-tag domain supplies explicit false
facts; tagged, crafted, rune and enchant lines retain their unresolved semantics. Damage
penetration and maximum resistance are different families and do not match these recipes.

The optional Rust source tests exercise authentic fresh parsing on the pinned constructed
catalog, including Ruby/Topaz Charm generated duplicates, numerical boundaries, nonempty
tags, negative catalyst quality, persistent advanced headers and predecessor retries.
Original rows are checked in complete item texts; that reference observation alone cannot
remove native source blockers or authorize XML overlays.

## Validation

`owned_extension_cli` exercises publication and the complete prior package chain, then the
new families' raw inputs, numerical components and all five original imports. The optional
source target is `cargo test -p poe-optimizer-pob --test owned_item_elemental_resistance --locked`.
All new authoring and tests are Rust; existing Python suites remain unchanged.
See the living [implementation record](../../../../../docs/implementation.md) for actual
run results, admission counts, remaining gates and the resume point.
